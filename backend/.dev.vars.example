# Copy to .dev.vars and set the selected provider's key. Never commit .dev.vars.
AI_PROVIDER=openai
OPENAI_API_KEY=

# Set AI_PROVIDER=gemini to try Gemini locally; restart npm run dev after changes.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash-lite
